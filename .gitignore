# Build output
bin/
obj/

# Test & coverage artifacts
artifacts/
TestResults/
*.trx
coverage.cobertura.xml

# Per-run quality report (regenerated by build.ps1 each run; do not track the timestamped output)
docs/QUALITY_REPORT.md

# Release binaries are distributed via GitHub Releases, not committed to git.
release/*.zip
release/*.exe
release/*.msi
release/*.msix

# User-specific
*.user
*.suo
.vs/

# Rider / VS Code
.idea/
.vscode/
